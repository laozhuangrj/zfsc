{
  "version": 2,
  "builds": [
    { "src": "_book/**", "use": "@vercel/static" }
  ],
  "routes": [
    { "handle": "filesystem" },
    {
      "src": "/(.*)",
      "dest": "/$1",
      "headers": { "cache-control": "s-maxage=1, stale-while-revalidate" }
    }
  ]
}
